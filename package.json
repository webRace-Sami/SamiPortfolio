{
  "dependencies": {
    "mongoose": "^8.19.1",
  "resend": "latest",
  "nodemailer": "^6.9.4"
  },
  "devDependencies": {
    "@types/node": "^24.8.1"
  }
  ,
  "scripts": {
    "build": "(cd frontend && npm install --no-audit --no-fund) && npm run build --prefix frontend && rm -rf dist && cp -a frontend/dist dist"
  }
}
