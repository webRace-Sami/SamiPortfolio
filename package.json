{
  "name": "sami-portfolio-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend && rm -rf dist && cp -a frontend/dist dist",
    "build:frontend": "npm run build --prefix frontend"
  }
}
